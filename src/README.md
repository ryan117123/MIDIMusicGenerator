# Source Modules

Place reusable project code here so notebooks stay clean and modular.

Suggested modules:

- `src/utils/data_io.py` for loading MIDI metadata and MIDI files
- `src/utils/tokenization.py` for event/token conversion
- `src/models/` for model definitions
- `src/utils/train_eval.py` for training loops and metrics
